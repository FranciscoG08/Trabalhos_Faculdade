<Page
    x:Class="Restaurante_UWP.Views.Mesas.ManageMesaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Restaurante_UWP.Views.Mesas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Restaurante.Domain.Models"
    mc:Ignorable="d">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#003366" Offset="0" />
            <GradientStop Color="#004080" Offset="0.5" />
            <GradientStop Color="#0066cc" Offset="1" />
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Mesas" Style="{StaticResource HeaderTextBlockStyle}" Margin="10,0,0,0" FontWeight="Bold" FontSize="35" Foreground="Gold"/>
            <CommandBar Grid.Column="1" VerticalAlignment="Center"
                        x:Name="mainCommandBar" Background="Transparent"
                        OverflowButtonVisibility="Collapsed"
                        DefaultLabelPosition="Right">
                <AppBarButton x:Name="btnNew" Icon="Add" Label="Mesa"
                    ToolTipService.ToolTip="Mesa"
                    Click="btnNew_Click"/>
            </CommandBar>
        </Grid>

        <!-- Lista de Mesas -->
        <ListView Grid.Row="1" ItemsSource="{x:Bind MesaViewModel.Mesas}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Mesa">
                    <Grid Margin="0, 5" Tapped="Grid_Tapped">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <!-- Ocupa o espaço restante -->
                            <ColumnDefinition Width="Auto"/>
                            <!-- Botões de ação -->
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Flyout para Mais Detalhes -->
                        <FlyoutBase.AttachedFlyout>
                            <Flyout>
                                <StackPanel>
                                    <TextBlock Text="Numero: "/>
                                    <TextBlock Text="{x:Bind Numero}"/>
                                    <TextBlock Text="Capacidade: "/>
                                    <TextBlock Text="{x:Bind Capacidade}"/>
                                </StackPanel>
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>

                        <!-- Exibir Numero e Capacidade da Mesa -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Número:" Margin="0,0,5,0"/>
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Numero}"/>

                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Capacidade:" Margin="0,0,5,0"/>
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Capacidade}"/>
                        </StackPanel>


                        <!-- Botão Excluir -->
                        <Button x:Name="btnDelete" Grid.Column="1" Margin="5, 0"
                                Click="btnDelete_Click" Background="Transparent">
                            <StackPanel>
                                <SymbolIcon Symbol="Delete" Foreground="Red"/>
                                <TextBlock Text="Excluir"/>
                            </StackPanel>
                        </Button>

                        <!-- Botão Editar -->
                        <Button x:Name="btnEdit" Grid.Column="2" Margin="5, 0"
                                Click="btnEdit_Click" Background="Transparent">
                            <StackPanel>
                                <SymbolIcon Symbol="Edit" Foreground="Green"/>
                                <TextBlock Text="Editar"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

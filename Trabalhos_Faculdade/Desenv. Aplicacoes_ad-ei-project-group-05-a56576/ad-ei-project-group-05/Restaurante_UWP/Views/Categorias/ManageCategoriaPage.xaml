<Page
    x:Class="Restaurante_UWP.Views.Categorias.ManageCategoriaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Restaurante_UWP.Views.Categorias"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Restaurante.Domain.Models"
    mc:Ignorable="d">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#003366" Offset="0" />
            <GradientStop Color="#004080" Offset="0.5" />
            <GradientStop Color="#0066cc" Offset="1" />
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Categorias" Style="{StaticResource HeaderTextBlockStyle}" Margin="10,0,0,0" FontWeight="Bold" FontSize="35" Foreground="Gold"/>
            <CommandBar Grid.Column="1" VerticalAlignment="Center"
            x:Name="mainCommandBar" Background="Transparent"
            OverflowButtonVisibility="Collapsed"
            DefaultLabelPosition="Right">
                <AppBarButton x:Name="btnNew" Icon="Add" Label="Categoria"
                    ToolTipService.ToolTip="New Category"
                    Click="btnNew_Click"/>
            </CommandBar>
        </Grid>
        <ListView Grid.Row="1" ItemsSource="{x:Bind CategoriaViewModel.Categorias}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Categoria">
                    <Grid Margin="0, 5" Tapped="Grid_Tapped">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <!--Flyout-->
                        <FlyoutBase.AttachedFlyout>
                            <Flyout>
                                <StackPanel>
                                    <TextBlock Text="Categoria"/>
                                    <TextBlock Text="{x:Bind Nome}"/>
                                </StackPanel>
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>
                        <!--Category Name-->
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}"
                           Text="{x:Bind Nome}"/>
                        <!--Delete Button-->
                        <Button x:Name="btnDelete" Grid.Column="1" Margin="5, 0"
                                Click="btnDelete_Click"  Background="Transparent">
                            <StackPanel>
                                <SymbolIcon Symbol="Delete" Foreground="red"/>
                                <TextBlock Text="Delete"/>
                            </StackPanel>
                        </Button>
                        <!--Edit Button-->
                        <Button x:Name="btnEdit" Grid.Column="2" Margin="5, 0"
                                Click="btnEdit_Click"  Background="Transparent">
                            <!--Evento associado Edit Button-->
                            <StackPanel>
                                <SymbolIcon Symbol="Edit" Foreground="Green"/>
                                <TextBlock Text="Edit"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
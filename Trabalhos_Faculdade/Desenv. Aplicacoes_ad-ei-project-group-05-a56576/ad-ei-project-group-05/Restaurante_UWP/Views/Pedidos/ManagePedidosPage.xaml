<Page
    x:Class="Restaurante_UWP.Views.Pedidos.ManagePedidosPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Restaurante_UWP.Views.Pedidos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:models="using:Restaurante.Domain.Models"
    mc:Ignorable="d">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#003366" Offset="0" />
            <GradientStop Color="#004080" Offset="0.5" />
            <GradientStop Color="#0066cc" Offset="1" />
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock 
                Text="Historico Pedidos" 
                Style="{StaticResource HeaderTextBlockStyle}" Margin="10,0,0,0"
                FontWeight="Bold"
                FontSize="35"
                Foreground="Gold"/>

            <CommandBar 
                Grid.Column="1" 
                VerticalAlignment="Center"
                x:Name="mainCommandBar" 
                Background="Transparent"
                OverflowButtonVisibility="Collapsed"
                DefaultLabelPosition="Right">
                <AppBarButton 
                    x:Name="btnNew" 
                    Icon="Add" 
                    Label="Pedido"
                    ToolTipService.ToolTip="Criar Novo Pedido"
                    Click="btnNew_Click" />
            </CommandBar>
        </Grid>

        <!-- Lista de pedidos -->
        <ListView Grid.Row="1" ItemsSource="{x:Bind PedidoViewModel.Pedidos}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Pedido">
                    <StackPanel Orientation="Vertical">
                        <!-- Nome -->
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Nome:" Margin="0,0,5,0"/>
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Clientes.Nome}" Margin="0,0,15,0"/>
                        </StackPanel>

                        <!-- Data -->
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Data:" Margin="0,0,5,0"/>
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Data}" Margin="0,0,15,0"/>
                        </StackPanel>

                        <!-- Empregado -->
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Empregado:" Margin="0,0,5,0"/>
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Empregados.Nome}" Margin="0,0,15,0"/>
                        </StackPanel>

                        <!-- Número da Mesa -->
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Número da Mesa:" Margin="0,0,5,0"/>
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Mesas.Numero}"/>
                        </StackPanel>

                        <!-- Botão Excluir -->
                        <Button x:Name="btnDelete" Margin="5,0" Click="btnDelete_Click" Background="Transparent">
                            <StackPanel>
                                <SymbolIcon Symbol="Delete" Foreground="Red"/>
                                <TextBlock Text="Excluir"/>
                            </StackPanel>
                        </Button>

                        <!-- Botão Editar -->
                        <Button x:Name="btnEdit" Margin="5,0" Click="btnEdit_Click" Background="Transparent">
                            <StackPanel>
                                <SymbolIcon Symbol="Edit" Foreground="Green"/>
                                <TextBlock Text="Editar"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>

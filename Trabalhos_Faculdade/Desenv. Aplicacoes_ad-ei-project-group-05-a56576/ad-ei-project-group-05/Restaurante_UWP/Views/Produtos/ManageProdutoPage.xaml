<Page
    x:Class="Restaurante_UWP.Views.Produtos.ManageProdutoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Restaurante_UWP.Views.Produtos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Restaurante.Domain.Models"
    mc:Ignorable="d">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#003366" Offset="0" />
            <GradientStop Color="#004080" Offset="0.5" />
            <GradientStop Color="#0066cc" Offset="1" />
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{x:Bind ProdutoViewModel.Title, Mode=OneTime}"
               Style="{StaticResource HeaderTextBlockStyle}" Margin="10,0,0,0" FontWeight="Bold" FontSize="35" Foreground="Gold"/>
            <CommandBar Grid.Column="1" VerticalAlignment="Center"
                    x:Name="mainCommandBar" Background="Transparent"
                    OverflowButtonVisibility="Collapsed"
                    DefaultLabelPosition="Right" Opened="mainCommandBar_Opened">
                <AppBarButton x:Name="btnNew" Icon="Add" Label="Produto"
                    ToolTipService.ToolTip="New Product"
                    Click="btnNew_Click" 
                    IsEnabled="{x:Bind ProdutoViewModel.Valid, Mode=OneWay}"/>
            </CommandBar>
        </Grid>
        <ListView Grid.Row="1" ItemsSource="{x:Bind ProdutoViewModel.Produtos}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Produto">
                    <Grid Margin="0, 5" Tapped="Grid_Tapped">
                        <FlyoutBase.AttachedFlyout>
                            <Flyout>
                                <StackPanel>
                                    <TextBlock Text="This is a Product"/>
                                    <TextBlock Text="{x:Bind Nome}"/>
                                </StackPanel>
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <!--Product Image-->
                        <Image Margin="10" x:Name="thumb"
                                Source="{x:Bind Thumb, Converter={StaticResource bytesToImageConverter}}" />
                        <!--Product Name-->
                        <StackPanel Grid.Column="1">
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}"
                                        Text="{x:Bind Nome}"
                                        Foreground="White" FontSize="27" FontFamily="Courier New"/>
                            
                            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Foreground="Gold">
                                    <Run Text="{x:Bind Preco, Mode=OneWay}" FontSize="20"/>
                                    <Run Text="€" FontSize="20"/>
                            </TextBlock>

                            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}"
                                       Text="{x:Bind Categoria.Nome}" FontSize="20"
                                       Foreground="White"/>
                        </StackPanel>
                        <!--Delete Button-->
                        <Button x:Name="btnDelete" Grid.Column="2" Margin="5, 0"
                                Click="btnDelete_Click"  Background="Transparent">
                            <StackPanel>
                                <SymbolIcon Symbol="Delete" Foreground="red"/>
                                <TextBlock Text="Delete"/>
                            </StackPanel>
                        </Button>
                        <!--Edit Button-->
                        <Button x:Name="btnEdit" Grid.Column="3" Margin="5, 0"
                                Click="btnEdit_Click"  Background="Transparent">
                            <!--Evento associado Edit Button-->
                            <StackPanel>
                                <SymbolIcon Symbol="Edit" Foreground="Green"/>
                                <TextBlock Text="Edit"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

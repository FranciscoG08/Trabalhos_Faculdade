<Page
    x:Class="Restaurante_UWP.Views.Empregados.ManageEmpregadosPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Restaurante_UWP.Views.Empregados"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Restaurante.Domain.Models"
    mc:Ignorable="d">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#003366" Offset="0" />
            <GradientStop Color="#004080" Offset="0.5" />
            <GradientStop Color="#0066cc" Offset="1" />
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Lista de Empregados" Style="{StaticResource HeaderTextBlockStyle}" Margin="10,0,0,0" FontWeight="Bold" FontSize="35" Foreground="Gold"/>
            <CommandBar Grid.Column="1" VerticalAlignment="Center"
                        x:Name="mainCommandBar" Background="Transparent"
                        OverflowButtonVisibility="Collapsed"
                        DefaultLabelPosition="Right">
            </CommandBar>
        </Grid>
        <ListView Grid.Row="1" ItemsSource="{x:Bind ListaEmpregados.Empregados}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Empregado">
                    <Grid Margin="0, 5" Tapped="Grid_Tapped">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <FlyoutBase.AttachedFlyout>
                            <Flyout>
                                <StackPanel>
                                    <TextBlock Text="Empregado: "/>
                                    <TextBlock Text="{x:Bind Nome}"/>
                                </StackPanel>
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>
                        
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Nome}" />
                        <Button x:Name="btnDelete" Grid.Column="1" Margin="200, 0"
                                 Click="btnDelete_Click" Background="Transparent">
                            <StackPanel>
                                <SymbolIcon Symbol="Delete" Foreground="Red" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                        
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Page>
